import Vue from "vue";
import { ActionTree, GetterTree, MutationTree, Action } from "vuex";

const state = {};

const getters: GetterTree<any, any> = {};

const mutations: MutationTree<any> = {};

const actions: ActionTree<any, any> = {
    // getUserTimeData({state}) {

    // }
};

export default {
  state,
  getters,
  mutations,
  actions,
};

